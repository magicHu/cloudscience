<% if user_signed_in? %>
  <%= link_to('退出', destroy_user_session_path, :method => :delete, :class => 'btn info') %>        
<% else %>
  <%= link_to('登录', new_user_session_path, :class => 'btn info')  %>  
<% end %>